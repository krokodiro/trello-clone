# One-click deploy on Render (free tier).
# After first deploy, set the 3 env vars listed in DEPLOY.md.

databases:
  - name: trello-db
    plan: free
    databaseName: trello
    user: trello

services:
  - type: keyvalue
    name: trello-redis
    plan: free
    ipAllowList: []

  - type: web
    name: trello-api
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: trello-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: trello-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: WEB_URL
        sync: false
      - key: SEED_ADMIN
        value: "true"
      - key: ADMIN_EMAIL
        value: admin@example.com
      - key: ADMIN_PASSWORD
        generateValue: true
      - key: ADMIN_NAME
        value: Admin

  - type: web
    name: trello-web
    runtime: docker
    rootDir: frontend
    plan: free
    envVars:
      - key: API_PUBLIC_URL
        sync: false
      - key: WS_PUBLIC_URL
        sync: false
